主張を検証する。主張: $ARGUMENTS

CLAUDE.md の「検証プロトコル」に従い、次の 5 段を順に実行し、各段の結果を見出し付きで報告せよ。
途中で「不可能」「仕様外」「できない」と書いてはならない。5 段を終えるまで結論は保留。

## 1. コミットの定義
主張に関係するハッシュ・署名・ID の計算関数を `~/tclk/src` から探し、定義を file:line で引用する。
その関数がどの検証（validate/decode/encode）を経由するか／しないかを明記する。

## 2. 入口の検証との区別
主張の根拠になっている「拒否」が、どの関数のどの行の検証で起きているかを file:line で示す。
その検証が 1 の計算に含まれるかを述べる。含まれないなら、それは主張の証拠にならない。

## 3. 第三者の実物の再現
同種の成果物を既に出している第三者がいれば（`/export` を grep）、その値を自分の理解で再計算する。
一致するまで進まない。一致しなければ「自分の理解が誤っている」として 1 に戻る。
比較対象が無ければ、無かった旨と探した範囲（部屋・期間・grep パターン）を書く。

## 4. 実行結果
Node/Python で実際に実行し、生の出力を貼る。表示するだけのコマンドは不可。

## 5. 結論
「X は F（file:line）では拒否される。プロトコル上は G により可能／不可能（根拠は 3 と 4）」の形で書く。
「不可能」と書く場合は、探した代替経路（エクスポート関数の一覧、手組みの canonical JSON、検証を経由しない経路）を列挙する。
相手が提示していない条件を補う案は、他に経路が無い場合にだけ、補う項目を明示して出す。
